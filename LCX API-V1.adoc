= LCX Exchange API Documentation
:author: LCX
:revnumber: 1.1.0
:revdate: 2026-03-21
:toc: left
:icons: font
:source-highlighter: highlight.js

== Introduction

LCX Exchange API oferă acces la date de piață, tranzacționare și cont printr-o interfață REST și WebSocket. Documentația acoperă versiunea *1.1.0*.

* Base URL REST: `https://exchange-api.lcx.com`
* Base URL Kline: `https://api-kline.lcx.com/v1/market/kline`
* WebSocket URL: `wss://exchange-api.lcx.com/`
* Header obligatoriu: `API-VERSION: 1.1.0`

=== Rate limiting

* Market API: 25 requesturi/secundă/IP
* Trading & Account API: 5 requesturi/secundă/IP și maxim 90 pe minut

=== Autentificare

Pentru endpoint-urile private (Trading, Account) se folosesc headerele:

* `x-access-key` – cheia API
* `x-access-sign` – semnătura HMAC-SHA256 (vezi formula mai jos)
* `x-access-timestamp` – timestamp în milisecunde

Semnătura se calculează astfel:

[source,js]
----
let requestString = method + endpoint + JSON.stringify(payload) // dacă payload gol: "{}"
let hash = CryptoJS.HmacSHA256(requestString, secret)
let signature = CryptoJS.enc.Base64.stringify(hash)
----

== 1. REST Public (Market Data)

=== Tabelă generală endpoint-uri publice

[cols="1,1,2,2", options="header"]
|===
| Metodă | Endpoint | Descriere | Parametri
| `GET` | `/api/book` | Cartea de ordine completă | `pair` (string, required)
| `GET` | `/v1/market/kline` | Lumânări OHLV | `pair`, `resolution`, `from`, `to` (secunde)
| `GET` | `/api/trades` | Trades publice recente | `pair`, `offset` (page, size 100)
| `GET` | `/api/pairs` | Toate perechile disponibile | -
| `GET` | `/api/pair` | Detalii pereche specifică | `pair` (string, required)
| `GET` | `/api/tickers` | Toate ticker-ele | -
| `POST` | `/api/ticker` | Ticker pentru o pereche | `pair` (query param)
|===

=== Exemplu: GET /api/book

==== Parametri
[source,json]
----
{
  "pair": "LCX/ETH"
}
----

==== Răspuns (succes)
[source,json]
----
{
  "status": "success",
  "message": "Successfully API response",
  "data": {
    "buy": [[0.022, 3], [0.02, 0]],
    "sell": []
  }
}
----

== 2. REST Privat (Trading)

Endpoint-urile de trading necesită autentificare cu headerele menționate.

=== Tabelă endpoint-uri trading

[cols="1,1,2,2", options="header"]
|===
| Metodă | Endpoint | Descriere | Corp / Parametri (query)
| `POST` | `/api/create` | Creează ordin | `Pair`, `Amount`, `Price` (limit), `OrderType` (LIMIT/MARKET), `Side` (BUY/SELL), `ClientOrderId` (opțional)
| `PUT` | `/api/modify` | Modifică ordin limită deschis | `OrderId`, `Amount`, `Price`
| `DELETE` | `/api/cancel` | Anulează un ordin | `orderId` (query)
| `DELETE` | `/order/cancel-all` | Anulează mai multe ordine | `orderIds` (array, max 25, query)
| `GET` | `/api/open` | Ordine deschise | `offset` (required), `pair`, `fromDate`, `toDate` (ms)
| `GET` | `/api/order` | Detalii ordin specific | `orderId` (query)
| `GET` | `/api/orderHistory` | Istoric ordine închise/cancelate | `offset` (required), `pair`, `fromDate`, `toDate`, `side`, `orderStatus`, `orderType`
| `GET` | `/api/uHistory` | Istoric propriu trades | `offset` (required), `pair`, `fromDate`, `toDate`
|===

=== Exemplu: POST /api/create

==== Corp request
[source,json]
----
{
  "Pair": "LCX/ETH",
  "Amount": 100,
  "Price": 0.004,
  "OrderType": "MARKET",
  "Side": "SELL"
}
----

==== Răspuns (succes)
[source,json]
----
{
  "status": "success",
  "message": "Order created successfully",
  "data": {
    "Id": "3dae4495-a219-40dc-99d8-99c49c1d3810",
    "Pair": "AVAX/USDC",
    "Price": 1,
    "Amount": 10,
    "Side": "SELL",
    "OrderType": "LIMIT",
    "Status": "OPEN"
  }
}
----

== 3. REST Privat (Account)

=== Tabelă endpoint-uri cont

[cols="1,1,2", options="header"]
|===
| Metodă | Endpoint | Descriere | Parametri
| `GET` | `/api/balances` | Toate soldurile | -
| `GET` | `/api/balance` | Sold pentru un coin specific | `coin` (query, required)
|===

=== Exemplu: GET /api/balance?coin=LCX

==== Răspuns
[source,json]
----
{
  "status": "success",
  "message": "Successfully Api response",
  "data": {
    "Coin": "LCX",
    "TotalBalance": 1011.02768906,
    "FreeBalance": 911.02768906,
    "OccupiedBalance": 100,
    "Decimals": 18
  }
}
----

== 4. WebSocket Public (Market)

Conexiunea se face la `wss://exchange-api.lcx.com/`. Pentru a menține conexiunea, trimite un mesaj `ping` la fiecare 60 de secunde.

=== Abonare la ticker

[source,json]
----
{
  "Topic": "subscribe",
  "Type": "ticker"
}
----

=== Abonare la orderbook

[source,json]
----
{
  "Topic": "subscribe",
  "Type": "orderbook"
}
----

=== Abonare la trade-uri publice

[source,json]
----
{
  "Topic": "subscribe",
  "Type": "trade"
}
----

Pentru dezabonare, folosește `"Topic": "unsubscribe"`.

== 5. WebSocket Privat (Autentificat)

Conexiunea se face la aceeași adresă, dar cu parametri de autentificare în URL:

`wss://exchange-api.lcx.com/?x-access-key=KEY&x-access-sign=SIGNATURE&x-access-timestamp=MS`

=== Abonare la portofel

[source,json]
----
{
  "Topic": "subscribe",
  "Type": "user_wallets"
}
----

=== Abonare la ordinele proprii

[source,json]
----
{
  "Topic": "subscribe",
  "Type": "user_orders"
}
----

=== Abonare la trade-urile proprii

[source,json]
----
{
  "Topic": "subscribe",
  "Type": "user_trades"
}
----

Toate abonamentele primesc snapshot inițial și actualizări incrementale.

== Note suplimentare

* Toate timestamp-urile în răspunsuri sunt în milisecunde (cu excepția endpoint-ului `/v1/market/kline` care folosește secunde).
* Pentru ordinele `MARKET`, câmpul `Price` nu este necesar.
* Pentru a genera semnătura, asigură-te că `requestString` folosește exact calea endpoint-ului (de exemplu `/api/create`) și corpul serializat fără spații suplimentare.

== Întrebări frecvente

*Cum obțin o cheie API?* – Accesează setările contului pe LCX Exchange și generează o pereche cheie/secret.

*Ce se întâmplă dacă depășesc limita de rate?* – Primești codul HTTP `429`.

*Pot folosi WebSocket-ul pentru a primi actualizări în timp real?* – Da, atât pentru date publice cât și pentru date private.
